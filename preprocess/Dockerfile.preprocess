FROM python:3.10-slim
WORKDIR /app
COPY . .
# 安装预处理服务需要的依赖
RUN pip install fastapi uvicorn python-multipart
# 启动模拟预处理服务，监听8001端口
CMD ["uvicorn", "fake_preprocess_api:app", "--host", "0.0.0.0", "--port", "8001"]