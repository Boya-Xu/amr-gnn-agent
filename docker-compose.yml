services:
  amr-gnn-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./experiments/checkpoints:/app/experiments/checkpoints
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1